# Implementation Plans

Generated by the improve skill. Plans 001 and 002 were written on 2026-09-23
(commit `7cbf391`), scoped to `src/components/ui/button.tsx`. Plans 003 to 008
were written on 2026-09-25 (commit `8d2892d`) from a color-theme audit.
Execute in the order below. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                       | Priority | Effort | Depends on | Status                                                                          |
| ---- | ----------------------------------------------------------- | -------- | ------ | ---------- | ------------------------------------------------------------------------------- |
| 001  | Disabled state inert + accessible `pending` state           | P1       | M      | —          | DONE (merged to main: `de1e0e8`, `dc8427a`, `397a10a`, plus test fix `0212bc7`) |
| 002  | `ref` support and single class-merge point for Button       | P3       | S      | 001        | DONE (merged to main: `d367dd2`)                                                |
| 003  | Give the paper ramp navy undertones and make ink neutral    | P1       | S      | —          | TODO                                                                            |
| 004  | Warm the dark end of the gold ramp so it reads amber        | P2       | S      | 003        | TODO                                                                            |
| 005  | Point `highlight` at gold, keep red for errors              | P2       | S      | 003, 004   | TODO                                                                            |
| 006  | Teach the contrast checker about sunken and raised surfaces | P2       | S      | 005        | TODO                                                                            |
| 007  | Replace the hard-coded dialog backdrop with a `veil` role   | P3       | S      | —          | TODO                                                                            |
| 008  | Sync the favicon and logo SVG colors to the ink ramp        | P3       | S      | 003        | TODO                                                                            |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 requires 001: both edit the `Button` function in `src/components/ui/button.tsx`, and 002 is written against the shape 001 produces.
- 003 and 004 both edit `scripts/color-ramps.mjs` and regenerate `src/styles/palette.css`, `src/styles/palette.generated.json` and `figma/manifest.json`. Run 004 after 003 so the generated files do not conflict. If they ever do, never hand-merge: keep the generator change, then re-run `pnpm color-ramps` and `pnpm figma-manifest`.
- 005 needs the 003 and 004 ramps: its contrast numbers were measured against them.
- 006 edits `scripts/contrast.mjs`, which 005 also edits (note strings), so it runs after 005.
- 007 is independent (it adds a role and a Tailwind mapping); its `roles.css` and manifest edits do not overlap the others, so it can run in parallel with anything.
- 008 copies ink hex values by hand, so it needs 003.
- After any of 003 to 007 lands, the Figma file is stale: the owner asks for a "re-sync Figma" from the new `figma/manifest.json` hash. Executors must not use Figma tools.

## Findings considered and rejected

- `.button--sm` is 36px, under the project's 44px rule: documented tradeoff in CLAUDE.md (compact controls only), unused outside `/styleguide`, passes WCAG 2.5.8 (24px).
- Missing `active:` pressed state and `whitespace-nowrap`: taste / speculative, no observed breakage.
- Lighter `accent-text` (gold-600) so links read gold instead of brown: fails 4.5:1 on `surface-sunken` in light mode; gold-700 is needed there.
- Dark-mode focus ring: `navy-400` on the new navy `paper-950` is 7.7:1, so the ring stays as is (roles do not change meaning).

## Not planned (direction, deferred)

- If the sticky mobile "Request" CTA from the homepage mock goes ahead, Button may need a full-width modifier (`.button--block`) and an icon/leading-content slot. Wait for the design to settle.
- Bringing red and green into the UI (a `success` role, "walk-ins welcome" status mark, flash-sheet ornament roles): the owner wants navy and gold foundations first, then a separate design pass.
- Dark-mode `surface-sunken` equals `background` (both `paper-950`, 1.00:1) and light-mode `accent-edge` and `highlight` are 2.63:1 on `surface-sunken`: the owner hand-tunes the surface ladder in `roles.css` while designing with it. Plan 006 records the light-mode gap in code.
- `subtle` equals `muted` and `scrim` equals `background`: placeholders the owner tunes by hand.
- Merging the `ink` and `paper` ramps into one `neutral` ramp (they are now both cool neutrals): possible later cleanup; it would rename ramps across roles, Figma and CLAUDE.md.
