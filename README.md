# El Camino Tattoos

Portfolio site for El Camino Tattoos. Next.js (App Router), React, Tailwind CSS, TypeScript. Hosted on Netlify.

## Development

```bash
npm install
npm run dev     # http://localhost:3000
npm run lint
npm run build
```

Fonts: Hanken Grotesk (variable) via `next/font/google`.
